"use client";

import Link from "next/link";
import {useEffect,useState} from "react";
type Suppression={id:number;type:string;value:string;reason:string;createdAt:string};
export default function SuppressionsPage(){const[rows,setRows]=useState<Suppression[]>([]);const[type,setType]=useState("domain");const[value,setValue]=useState("");const[reason,setReason]=useState("Do not contact");const[error,setError]=useState<string|null>(null);const[saving,setSaving]=useState(false);async function load(){const res=await fetch("/api/suppressions",{cache:"no-store"});const body=await res.json();if(!res.ok)throw new Error(body.error??"Failed to load suppressions");setRows(body.suppressions??[]);}useEffect(()=>{load().catch(e=>setError(e instanceof Error?e.message:String(e)));},[]);async function add(){if(!value.trim())return;setSaving(true);setError(null);try{const res=await fetch("/api/suppressions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({type,value,reason})});const body=await res.json();if(!res.ok)throw new Error(body.error??"Failed to add suppression");setValue("");await load();}catch(e){setError(e instanceof Error?e.message:String(e));}finally{setSaving(false);}}async function remove(id:number){setSaving(true);setError(null);try{const res=await fetch(`/api/suppressions/${id}`,{method:"DELETE"});const body=await res.json();if(!res.ok)throw new Error(body.error??"Failed to remove suppression");await load();}catch(e){setError(e instanceof Error?e.message:String(e));}finally{setSaving(false);}}return <main className="min-h-screen bg-zinc-950 text-white"><div className="mx-auto max-w-5xl px-4 py-8"><div className="mb-8 flex items-start justify-between gap-4"><div><h1 className="text-2xl font-bold">Suppressions</h1><p className="mt-1 text-sm text-zinc-400">Global do-not-contact rules are checked again at the backend send boundary.</p></div><Link href="/settings" className="rounded border border-zinc-700 bg-zinc-800 px-3 py-2 text-sm">Settings</Link></div>{error&&<div className="mb-5 rounded border border-red-900 bg-red-950/40 px-4 py-3 text-sm text-red-300">{error}</div>}<section className="mb-6 rounded-xl border border-zinc-800 bg-zinc-900 p-5"><h2 className="font-semibold">Add suppression</h2><div className="mt-4 grid gap-3 md:grid-cols-[160px_1fr_1fr_auto]"><select value={type} onChange={e=>setType(e.target.value)} className="rounded border border-zinc-700 bg-zinc-950 px-3 py-2 text-sm"><option value="email">Email</option><option value="domain">Domain</option><option value="category">Category</option></select><input value={value} onChange={e=>setValue(e.target.value)} placeholder={type==="email"?"owner@example.com":type==="domain"?"example.com":"roofing"} className="rounded border border-zinc-700 bg-zinc-950 px-3 py-2 text-sm"/><input value={reason} onChange={e=>setReason(e.target.value)} placeholder="Reason" className="rounded border border-zinc-700 bg-zinc-950 px-3 py-2 text-sm"/><button disabled={saving||!value.trim()} onClick={add} className="rounded bg-red-800 px-4 py-2 text-sm disabled:opacity-50">Add</button></div></section><section className="rounded-xl border border-zinc-800 bg-zinc-900"><div className="border-b border-zinc-800 px-5 py-4"><h2 className="font-semibold">Active suppression rules</h2></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="text-left text-xs uppercase text-zinc-500"><tr><th className="px-4 py-3">Type</th><th>Value</th><th>Reason</th><th>Added</th><th></th></tr></thead><tbody className="divide-y divide-zinc-800">{rows.length?rows.map(row=><tr key={row.id}><td className="px-4 py-3 capitalize">{row.type}</td><td className="font-medium">{row.value}</td><td className="text-zinc-400">{row.reason}</td><td className="text-zinc-500">{new Date(row.createdAt).toLocaleString()}</td><td className="pr-4 text-right"><button disabled={saving} onClick={()=>remove(row.id)} className="rounded bg-zinc-800 px-3 py-1 text-xs text-red-300 disabled:opacity-50">Remove</button></td></tr>):<tr><td colSpan={5} className="px-4 py-8 text-center text-zinc-500">No global suppressions.</td></tr>}</tbody></table></div></section></div></main>;}
